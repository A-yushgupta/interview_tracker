<!DOCTYPE html>
<html lang="en">
<%- include('./partials/header.ejs') %>

<body>

  
    
    <%- include('./partials/nav.ejs') %>
    <div class="blogContent">
        
        <% if(blogs.length>0){ %>
            <h2>All blogs</h2>
            <%  blogs.forEach(blog => {%>
                <h3 class="title"><%= blog.title %></h3>
                <p class="snippet"> <%= blog.snippet %></p>
                
                <%})%>
                <%} else {%>
                    <p>threere are no blogs.</p>
                    <%}%>
                </div>
                <%- include('./partials/footer.ejs') %>
</body>
</html>